<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="ForceAutoHDR.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ForceAutoHDR.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ForceAutoHDR"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--
            No IconSource here: an ms-appx image would mean shipping the .ico as a loose file
            (publish does not copy it) or baking 370 KB into the PRI. The window icon is taken
            from the one already embedded in the exe — see MainWindow.xaml.cs.
        -->
        <TitleBar x:Name="AppTitleBar" Title="ForceAutoHDR" />

        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
